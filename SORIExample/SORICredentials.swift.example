// Copy this file to SORICredentials.swift and replace both placeholders.
// Never commit SORICredentials.swift or a live Secret Key to any repository.

enum SORICredentials {
    static let applicationID = "YOUR_SORI_APPLICATION_ID"
    static let secretKey = "YOUR_SORI_SECRET_KEY"
}
